services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=mydatabase'
      - 'MYSQL_PASSWORD=root'
      - 'MYSQL_ROOT_PASSWORD='
      - 'MYSQL_USER=myuser'
    ports:
      - '3306'
